package org.child1.JUnitTesting.service.TestVoidFunc;

public class Main {
    public static void main(String[] args) {
        //We see the deleteProduct in Product Service deletes a product by calling the
        //deleteById in product repository, but the deleteById in product repository returns
        //void so how to test it as we have prev seen that we were testing using when and
        //theReturn as it was returning something. Let's see by adding test for it.
    }
}
